#!/usr/bin/env node// Tests for generate-cli-examples.jsconst assert = require('node:assert');const test = require('node:test');const fs = require('fs');const path = require('path');const { execSync } = require('child_process');const TEST_DIR = path.join(__dirname, '..', 'test-fixtures-cli-examples');const SCRIPT_PATH = path.join(__dirname, '..', 'generate-cli-examples.js');// Setup: Create test fixture directory with mock version folder and tools-list.jsontest.before(() => {  // Clean up any existing test dir  if (fs.existsSync(TEST_DIR)) {    fs.rmSync(TEST_DIR, { recursive: true, force: true });  }    fs.mkdirSync(TEST_DIR, { recursive: true });    // Create a mock version directory  const versionDir = path.join(TEST_DIR, '3.0.0-beta.99+testversion');  fs.mkdirSync(versionDir, { recursive: true });    // Create a mock tools-list.json  const mockToolsList = {    results: [      {        command: 'storage account list',        option: [          { name: '--resource-group', required: true },          { name: '--subscription', required: false },          { name: '--output', required: false }        ]      },      {        command: 'storage blob upload',        option: [          { name: '--account-name', required: true },          { name: '--container', required: true },          { name: '--file', required: true },          { name: '--tenant', required: false }        ]      },      {        command: 'keyvault secret get',        option: [          { name: '--vault-name', required: true },          { name: '--name', required: true },          { name: '--learn', required: false }        ]      }    ]  };    fs.writeFileSync(    path.join(versionDir, 'tools-list.json'),    JSON.stringify(mockToolsList, null, 2),    'utf8'  );});// Cleanuptest.after(() => {  if (fs.existsSync(TEST_DIR)) {    fs.rmSync(TEST_DIR, { recursive: true, force: true });  }});test('generate-cli-examples outputs to version folder, not root', (t) => {  // Copy the script to test dir temporarily  const testScriptPath = path.join(TEST_DIR, 'generate-cli-examples.js');  fs.copyFileSync(SCRIPT_PATH, testScriptPath);    // Run the script from the test directory  execSync(`node ${testScriptPath}`, { cwd: TEST_DIR, encoding: 'utf8' });    // Assert: cli-examples.md should exist in version folder  const versionDir = '3.0.0-beta.99+testversion';  const expectedPath = path.join(TEST_DIR, versionDir, 'cli-examples.md');  const rootPath = path.join(TEST_DIR, 'cli-examples.md');    assert.ok(fs.existsSync(expectedPath), `cli-examples.md should exist at ${expectedPath}`);  assert.ok(!fs.existsSync(rootPath), `cli-examples.md should NOT exist at root (${rootPath})`);    // Clean up copied script  fs.unlinkSync(testScriptPath);});test('generate-cli-examples creates valid markdown with namespaces', (t) => {  // Copy the script to test dir temporarily  const testScriptPath = path.join(TEST_DIR, 'generate-cli-examples.js');  fs.copyFileSync(SCRIPT_PATH, testScriptPath);    // Run the script from the test directory  execSync(`node ${testScriptPath}`, { cwd: TEST_DIR, encoding: 'utf8' });    // Read the generated file  const versionDir = '3.0.0-beta.99+testversion';  const filePath = path.join(TEST_DIR, versionDir, 'cli-examples.md');  const content = fs.readFileSync(filePath, 'utf8');    // Assert: File should contain namespace headers  assert.ok(content.includes('## storage'), 'Should have storage namespace header');  assert.ok(content.includes('## keyvault'), 'Should have keyvault namespace header');    // Assert: File should contain command examples  assert.ok(content.includes('azmcp storage account list'), 'Should have storage account list command');  assert.ok(content.includes('azmcp storage blob upload'), 'Should have storage blob upload command');  assert.ok(content.includes('azmcp keyvault secret get'), 'Should have keyvault secret get command');    // Assert: Required params should be bare (not in brackets)  assert.ok(content.includes('--resource-group <resource-group>'), 'Required params should not be in brackets');  assert.ok(content.includes('--account-name <account-name>'), 'Required params should not be in brackets');    // Assert: Optional params should be in brackets  assert.ok(content.includes('[--output <output>]'), 'Optional params should be in brackets');    // Assert: Global switches should be filtered out  assert.ok(!content.includes('--subscription'), 'Global switches should be filtered');  assert.ok(!content.includes('--tenant'), 'Global switches should be filtered');  assert.ok(!content.includes('--learn'), 'Global switches should be filtered');    // Clean up copied script  fs.unlinkSync(testScriptPath);});test('generate-cli-examples returns object with version directory info', (t) => {  // This tests that findLatestToolsList returns { toolsPath, versionDir }  // We'll indirectly test this by checking the console output contains the version dir  const testScriptPath = path.join(TEST_DIR, 'generate-cli-examples.js');  fs.copyFileSync(SCRIPT_PATH, testScriptPath);    const output = execSync(`node ${testScriptPath}`, { cwd: TEST_DIR, encoding: 'utf8' });    // Assert: Output should mention the version directory in the path  assert.ok(output.includes('3.0.0-beta.99+testversion'), 'Output should mention version directory');  assert.ok(output.includes('/cli-examples.md'), 'Output should show relative path with version dir');    // Clean up  fs.unlinkSync(testScriptPath);});