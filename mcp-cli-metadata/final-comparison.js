const fs = require('fs');function extractJSON(content) {  const startIdx = content.indexOf('{');  const endIdx = content.lastIndexOf('}');  if (startIdx === -1 || endIdx === -1) return null;  return JSON.parse(content.substring(startIdx, endIdx + 1));}// Load versionsconst beta40 = extractJSON(fs.readFileSync('cli-beta40.json', 'utf8'));const beta3v3 = extractJSON(fs.readFileSync('cli-beta3.json', 'utf8'));console.log('=== 2.0.0-beta.40 vs 3.0.0-beta.3 DETAILED COMPARISON ===\n');// Index by commandfunction buildToolIndex(results) {  const index = {};  if (!results) return index;  results.forEach(tool => {    index[tool.command] = tool;  });  return index;}const toolIdx40 = buildToolIndex(beta40.results);const toolIdx3v3 = buildToolIndex(beta3v3.results);// Find all commandsconst allCmds = new Set([...Object.keys(toolIdx40), ...Object.keys(toolIdx3v3)]);const commands = Array.from(allCmds).sort();console.log(`Commands in 2.x beta.40: ${Object.keys(toolIdx40).length}`);console.log(`Commands in 3.x beta.3: ${Object.keys(toolIdx3v3).length}`);console.log(`Total unique: ${commands.length}\n`);const changes = {  parameterAdded: [],  parameterRemoved: [],  descriptionChanged: [],  newCommands: [],  removedCommands: [],  requiredChanged: [],  identical: 0};commands.forEach(cmd => {  const tool40 = toolIdx40[cmd];  const tool3v3 = toolIdx3v3[cmd];  if (!tool40) {    changes.newCommands.push(cmd);    return;  }    if (!tool3v3) {    changes.removedCommands.push(cmd);    return;  }  // Compare descriptions  const desc40 = (tool40.description || '').trim();  const desc3 = (tool3v3.description || '').trim();  if (desc40 !== desc3) {    changes.descriptionChanged.push({      command: cmd,      desc40: desc40.substring(0, 90),      desc3: desc3.substring(0, 90)    });  }  // Compare parameters  const params40 = (tool40.option || []).map(o => o.name).sort();  const params3 = (tool3v3.option || []).map(o => o.name).sort();  const added = params3.filter(p => !params40.includes(p));  const removed = params40.filter(p => !params3.includes(p));  if (added.length > 0) {    changes.parameterAdded.push({      command: cmd,      added    });  }  if (removed.length > 0) {    changes.parameterRemoved.push({      command: cmd,      removed    });  }  // Check required status changes  const required40 = {};  const required3 = {};  (tool40.option || []).forEach(o => required40[o.name] = o.required || false);  (tool3v3.option || []).forEach(o => required3[o.name] = o.required || false);    const common = params40.filter(p => params3.includes(p));  common.forEach(p => {    if ((required40[p] || false) !== (required3[p] || false)) {      changes.requiredChanged.push({        command: cmd,        param: p,        from: required40[p] ? 'required' : 'optional',        to: required3[p] ? 'required' : 'optional'      });    }  });  if (added.length === 0 && removed.length === 0 && desc40 === desc3 && changes.requiredChanged.filter(c => c.command === cmd).length === 0) {    changes.identical++;  }});console.log(`=== SUMMARY ===`);console.log(`Identical tools: ${changes.identical}`);console.log(`Tools with description changes: ${changes.descriptionChanged.length}`);console.log(`Tools with parameter additions: ${changes.parameterAdded.length}`);console.log(`Tools with parameter removals: ${changes.parameterRemoved.length}`);console.log(`Tools with required status changes: ${new Set(changes.requiredChanged.map(c => c.command)).size}`);console.log(`New tools in 3.x: ${changes.newCommands.length}`);console.log(`Tools removed from 2.x: ${changes.removedCommands.length}`);if (changes.descriptionChanged.length > 0) {  console.log('\n=== DESCRIPTION CHANGES (sample) ===');  changes.descriptionChanged.slice(0, 10).forEach(item => {    console.log(`\n${item.command}:`);    console.log(`  2.x: ${item.desc40}...`);    console.log(`  3.x: ${item.desc3}...`);  });  if (changes.descriptionChanged.length > 10) {    console.log(`\n... and ${changes.descriptionChanged.length - 10} more description changes`);  }}if (changes.parameterAdded.length > 0) {  console.log('\n=== PARAMETER ADDITIONS ===');  changes.parameterAdded.slice(0, 10).forEach(item => {    console.log(`${item.command}: +[${item.added.join(', ')}]`);  });  if (changes.parameterAdded.length > 10) {    console.log(`... and ${changes.parameterAdded.length - 10} more tools with additions`);  }}if (changes.parameterRemoved.length > 0) {  console.log('\n=== PARAMETER REMOVALS ===');  changes.parameterRemoved.slice(0, 10).forEach(item => {    console.log(`${item.command}: -[${item.removed.join(', ')}]`);  });  if (changes.parameterRemoved.length > 10) {    console.log(`... and ${changes.parameterRemoved.length - 10} more tools with removals`);  }}if (changes.requiredChanged.length > 0) {  console.log('\n=== REQUIRED STATUS CHANGES ===');  changes.requiredChanged.slice(0, 15).forEach(item => {    console.log(`${item.command}: ${item.param} (${item.from} → ${item.to})`);  });  if (changes.requiredChanged.length > 15) {    console.log(`... and ${changes.requiredChanged.length - 15} more parameter status changes`);  }}// Save full reportfs.writeFileSync('2x-beta40-vs-3x-beta3.json', JSON.stringify({  version2: '2.0.0-beta.40',  version3: '3.0.0-beta.3',  summary: {    identical: changes.identical,    descriptionChanges: changes.descriptionChanged.length,    parameterAdditions: changes.parameterAdded.length,    parameterRemovals: changes.parameterRemoved.length,    requiredStatusChanges: changes.requiredChanged.length,    newTools: changes.newCommands.length,    removedTools: changes.removedCommands.length  },  changes}, null, 2));console.log('\n\nFull report saved to: 2x-beta40-vs-3x-beta3.json');