const { execSync } = require('child_process');try {  const versions = JSON.parse(execSync('npm view @azure/mcp versions --json').toString());    // Filter for 2.0.0 beta versions  const v2 = versions.filter(v => v.startsWith('2.0.0-beta'));  console.log('2.0.0-beta versions available on npm:');  v2.forEach(v => console.log(`  ${v}`));    console.log('\n3.0.0-beta versions available on npm:');  const v3 = versions.filter(v => v.startsWith('3.0.0-beta'));  v3.forEach(v => console.log(`  ${v}`));    console.log('\nLatest version:', versions[versions.length - 1]);} catch (err) {  console.error('Error:', err.message);}