List supported programming languages for Azure Functions development. Use to discover available languages, compare options, or choose a language to get started. Returns language names, runtime versions, prerequisites, development tools, and init/run/build commands. Start here before using functions project get and functions template get.

### Example CLI commands

Basic usage:

```azurecli
azmcp functions language list
```

This tool has no CLI parameters.

