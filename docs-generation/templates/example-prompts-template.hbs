{{!-- 
Handlebars Template for Example Prompts Section
This template generates the "Example prompts include:" section for Azure MCP Server tool documentation.

Input Context Object:
{
  toolName: "The friendly name of the tool",
  toolCommand: "<!-- the html comment command -->",
  actionVerb: "The primary action verb (list, create, get, etc.)",
  resourceType: "The Azure resource type",
  parameters: [
    {
      name: "Parameter name",
      required: true/false,
      description: "Parameter description"
    }
  ],
  examplePrompts: [
    {
      category: "Category label (e.g., 'List accounts')",
      prompt: "The example prompt text"
    }
  ]
}

Usage:
1. Populate the context object with your tool's information
2. Generate example prompts using the master-prompt-generator.md
3. Add the generated prompts to the examplePrompts array
4. Render this template with your context object

--}}

Example prompts include:

{{#each examplePrompts}}
- **{{category}}**: "{{prompt}}"
{{/each}}

{{!-- 
If parameters exist, add the parameters table after the example prompts 
--}}
{{#if parameters}}

| Parameter | Required or optional | Description |
|-----------|-------------|-------------|
{{#each parameters}}
| **{{name}}** | {{#if required}}Required{{else}}Optional{{/if}} | {{description}} |
{{/each}}
{{/if}}

{{!-- 
TEMPLATE USAGE EXAMPLES:

Example 1: Simple tool with no parameters (List Subscriptions)
-----------------------------------------------------------
Context:
{
  toolName: "List subscriptions",
  toolCommand: "<!-- subscription list -->",
  actionVerb: "list",
  resourceType: "subscriptions",
  parameters: [],
  examplePrompts: [
    { category: "List subscriptions", prompt: "Show me all of my subscriptions." },
    { category: "Find subscriptions", prompt: "List all subscriptions starting with `northeast`." }
  ]
}

Output:
Example prompts include:

- **List subscriptions**: "Show me all of my subscriptions."
- **Find subscriptions**: "List all subscriptions starting with `northeast`."


Example 2: Complex tool with multiple required parameters (Create Storage Account)
-----------------------------------------------------------
Context:
{
  toolName: "Create storage account",
  toolCommand: "<!-- storage account create -->",
  actionVerb: "create",
  resourceType: "storage account",
  parameters: [
    { name: "Resource group", required: true, description: "The name of the Azure resource group. This is a logical container for Azure resources." },
    { name: "Account", required: true, description: "The name of the Azure Storage account to create. Must be globally unique, 3-24 characters, lowercase letters, and numbers only." },
    { name: "Location", required: true, description: "The Azure region where the storage account will be created (for example, `eastus`, `westus2`)." },
    { name: "SKU", required: false, description: "The storage account SKU. Valid values: `Standard_LRS`, `Standard_GRS`, etc." }
  ],
  examplePrompts: [
    { category: "Create account", prompt: "Create a storage account named 'mystorageaccount' in resource group 'my-resource-group' in location 'eastus'." },
    { category: "New storage", prompt: "Set up a new storage account called 'datastore2025' in resource group 'my-resource-group' in region 'westus2'." },
    { category: "Specify SKU and kind", prompt: "Create a storage account 'mydata' in resource group 'my-resource-group' in location 'eastus' with Standard_GRS." }
  ]
}

Output:
Example prompts include:

- **Create account**: "Create a storage account named 'mystorageaccount' in resource group 'my-resource-group' in location 'eastus'."
- **New storage**: "Set up a new storage account called 'datastore2025' in resource group 'my-resource-group' in region 'westus2'."
- **Specify SKU and kind**: "Create a storage account 'mydata' in resource group 'my-resource-group' in location 'eastus' with Standard_GRS."

| Parameter | Required or optional | Description |
|-----------|-------------|-------------|
| **Resource group** | Required | The name of the Azure resource group. This is a logical container for Azure resources. |
| **Account** | Required | The name of the Azure Storage account to create. Must be globally unique, 3-24 characters, lowercase letters, and numbers only. |
| **Location** | Required | The Azure region where the storage account will be created (for example, `eastus`, `westus2`). |
| **SKU** | Optional | The storage account SKU. Valid values: `Standard_LRS`, `Standard_GRS`, etc. |


Example 3: Tool with mix of required and optional parameters (Get Key Vault Secret)
-----------------------------------------------------------
Context:
{
  toolName: "Get secret",
  toolCommand: "<!-- keyvault secret get -->",
  actionVerb: "get",
  resourceType: "secret",
  parameters: [
    { name: "Vault", required: true, description: "The name of the Key Vault." },
    { name: "Name", required: true, description: "The name of the secret to retrieve." }
  ],
  examplePrompts: [
    { category: "Get specific secret", prompt: "Retrieve the 'database-connection-string' secret from my 'production-vault' Key Vault." },
    { category: "Access API key", prompt: "Get the 'third-party-api-key' secret from the 'api-secrets' vault" },
    { category: "Check secret value", prompt: "What is the value of the 'ssl-certificate-password' secret in my Key Vault?" }
  ]
}

Output:
Example prompts include:

- **Get specific secret**: "Retrieve the 'database-connection-string' secret from my 'production-vault' Key Vault."
- **Access API key**: "Get the 'third-party-api-key' secret from the 'api-secrets' vault"
- **Check secret value**: "What is the value of the 'ssl-certificate-password' secret in my Key Vault?"

| Parameter | Required or optional | Description |
|-----------|-------------|-------------|
| **Vault** | Required | The name of the Key Vault. |
| **Name** | Required | The name of the secret to retrieve. |

--}}
