Please review and clean up the following Azure MCP tool family documentation file to ensure it meets Microsoft style guide standards.

**Filename**: {{FILENAME}}

**Current Content**:
```markdown
{{CONTENT}}
```

**Your Task**:
1. Review the content for adherence to Microsoft style guide standards
2. Improve clarity, readability, and technical accuracy
3. Ensure consistent formatting and structure
4. Apply Azure MCP-specific documentation conventions
5. **Convert any `--switch-name` references in tool description text to natural language** (e.g., `--resource-group` → "resource group") unless the switch name is a parameter keyword in the tool's parameter table
6. **Remove LLM-specific tool-calling guidance** from descriptions — this documentation is for people using Azure MCP Server from their IDE, not for LLMs calling tools. Strip "Use this tool when...", "Do not use this tool to...", "equivalent to `az ...`", tool disambiguation hints, and parameter-passing instructions. Rewrite to focus on what a human user can accomplish
7. **Backtick formatting consistency** — If a term in the tool description also appears in the parameter descriptions table enclosed in single backticks, apply the same backtick formatting to that term in the description text (e.g., if the parameter table uses `resource-group` in backticks, the description must also use `resource-group` with backticks)
8. Output ONLY the cleaned markdown - no explanations or commentary

**Remember**:
- Preserve all frontmatter (YAML metadata)
- Keep all include references
- Maintain all links and technical content
- Focus on style improvements and clarity
- Output must be valid, production-ready markdown
