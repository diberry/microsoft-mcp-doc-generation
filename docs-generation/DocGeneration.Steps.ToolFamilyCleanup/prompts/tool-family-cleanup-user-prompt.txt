**Your Task**:
1. Review the content for adherence to Microsoft style guide standards
2. **CRITICAL: Ensure ALL required parameters appear in at least 3 example prompts**
3. Add missing content sections (Overview, Prerequisites, Success verification, Related content)
4. Improve clarity, readability, and technical accuracy
5. Ensure consistent formatting and structure
6. Apply Azure MCP-specific documentation conventions
7. Fix metadata/frontmatter to use `concept-article` format
8. **Convert any `--switch-name` references in tool description text to natural language** (e.g., `--resource-group` → "resource group") unless the switch name is a parameter keyword in the tool's parameter table
9. **Remove LLM-specific tool-calling guidance** from descriptions — strip "Use this tool when...", "Do not use this tool to...", "equivalent to `az ...`", tool disambiguation hints, and parameter-passing instructions. Rewrite to focus on what a human user can accomplish
10. **Backtick formatting consistency** — If a term in the tool description also appears in the parameter descriptions table enclosed in single backticks, apply the same backtick formatting to that term in the description text (e.g., if the parameter table uses `resource-group` in backticks, the description must also use `resource-group` with backticks)
11. Output ONLY the cleaned markdown - no explanations or commentary

**Filename**: {{FILENAME}}

**Current Content**:
```markdown
{{CONTENT}}
```

**Your Task**:
1. Review the content for adherence to Microsoft style guide standards
2. **CRITICAL: Ensure ALL (100%) required parameters appear in at least 3 example prompts**
3. Add missing content sections (Overview, Related content)
4. Improve clarity, readability, and technical accuracy
5. Ensure consistent formatting and structure
6. Apply Azure MCP-specific documentation conventions
7. Fix metadata/frontmatter to use `concept-article` format
8. **Convert any `--switch-name` references in tool description text to natural language** (e.g., `--resource-group` → "resource group")
9. **Remove LLM-specific tool-calling guidance** from descriptions — this documentation is for people using Azure MCP Server from their IDE, not for LLMs calling tools
10. Output ONLY the cleaned markdown - no explanations or commentary

**Remember**:
- Preserve all frontmatter (YAML metadata)
- Keep all include references
- Maintain all links and technical content
- Focus on style improvements and clarity
- Output must be valid, production-ready markdown
