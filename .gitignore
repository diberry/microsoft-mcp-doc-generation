.dist/
.work/
tests/test-cmds.txt
.testsettings.json

bin/
obj/
.vs/
.idea/
TestResults/
*.binlog

node_modules/
.npmrc

.env
.env.local
.env.*.local
.env.production
.env*
**/.env**

generated*/
generated*
pilot-output/
prototype/

/docs/commandline
git

# Pipeline trace output (generated per run, not committed)
**/trace/

# LKG baseline directories (created during pipeline validation, not committed)
lkg-*/

*.log

# Live CLI output (generated by pretest, not committed)
**/cli-output-live.json

# Workflow-generated diff files (uploaded as artifacts, not committed)
mcp-cli-metadata/diff-*.txt
mcp-cli-metadata/environment.txt

# Local-only orchestration scripts (not committed)
start-with-*.sh

# Temp directories created by step scripts during generation
docs-generation/scripts/temp*/

# Cloned Agent Skills repo (fetched fresh at generation time)
docs-generation/skills-source/
skills-source/
# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream
skills-generation/TestResults/

vale_bin/
