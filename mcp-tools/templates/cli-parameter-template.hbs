{{#if hasParameters}}
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
{{#each switches}}
| `{{DisplayName}}` | {{Type}} | {{#if IsRequired}}Yes{{else}}No{{/if}} | {{{escapeTableCell Description}}} |
{{/each}}
{{else}}
This tool has no CLI parameters.
{{/if}}
