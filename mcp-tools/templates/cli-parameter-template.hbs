{{#if hasParameters}}
| Parameter | Type | Description |
|-----------|------|-------------|
{{#each switches}}
| `{{Name}}` | {{Type}} | {{{escapeTableCell Description}}} |
{{/each}}
{{else}}
This tool has no CLI parameters.
{{/if}}
