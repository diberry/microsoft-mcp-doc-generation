Generate the primary documentation link and additional links for a how-to article about managing {{serviceBrandName}} with Azure MCP Server.

**Service Information:**
- Service Brand Name: {{serviceBrandName}}
- Service Identifier: {{serviceIdentifier}}
- Tools Reference Link: {{toolsReferenceLink}}

**Available MCP Tools for this service (complete capability boundary):**
{{#each tools}}
- {{command}} — {{description}}
{{/each}}

## Your Task

Use the listed commands as the scope boundary. Do not select documentation for broader service capabilities that these tools do not support.

1. **genai-serviceDocLink**: Path to the official Azure documentation page that most directly covers {{serviceBrandName}} and the listed tool operations. Remove the `https://learn.microsoft.com/en-us` prefix. Leave empty if unsure.
2. **genai-additionalLinks**: 2-4 additional documentation links directly relevant to the listed tool operations. Leave any uncertain URL empty rather than fabricating one.

## Output Format

Return ONLY valid JSON with exactly these top-level keys:
- `genai-serviceDocLink`
- `genai-additionalLinks`

No markdown code blocks, no explanations. The response must begin with `{` and end with `}`.
