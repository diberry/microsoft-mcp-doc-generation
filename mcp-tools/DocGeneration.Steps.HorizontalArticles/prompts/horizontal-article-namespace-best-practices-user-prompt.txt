Generate best practices for a how-to article about managing {{serviceBrandName}} with Azure MCP Server.

**Service Information:**
- Service Brand Name: {{serviceBrandName}}
- Service Identifier: {{serviceIdentifier}}

**Available MCP Tools for this service (compact list):**
{{#each tools}}
- {{command}} — {{description}}
{{/each}}

## Your Task

**genai-bestPractices**: 2-5 best practices scoped to using the tools above effectively. Reference only exact tool commands from the list when giving advice. Never turn a parameter or action value into a subcommand; include its `--option-name` on the exact listed command (for example, `command --action update`, never `command update`). Repeat the complete exact command path before every option occurrence; never shorten alternatives to `command --action update or --action generate`. After an exact command path, use only an option or punctuation; don't append words such as `command`, `tool`, or `action`. Priority: security first, then safe usage, then efficiency.

## Output Format

Return ONLY valid JSON with exactly this top-level key:
- `genai-bestPractices`

No markdown code blocks, no explanations. The response must begin with `{` and end with `}`.
